# n8n-scripts

Remote Code-node scripts for the Figma MCP → AI include HTML → GitHub → Vercel workflow.

Loaded at runtime via HTTP (no `fs` in n8n):

`https://raw.githubusercontent.com/donghobridge/figma-mcp-test/main/n8n-scripts/`
